# Required Command Layer configuration
SEEDER_INTERNAL_TOKEN=
PLAYBACK_JWT_ISSUER=https://sonexus.club
# Use an RSA public key in PEM format. Escaped newlines (\n) are supported.
PLAYBACK_JWT_PUBLIC_KEY=
CORS_ALLOWED_ORIGINS=https://sonexus.club

# Approved defaults
PLAYBACK_JWT_AUDIENCE=sonexus-gateway
SEEDER_REQUEST_TIMEOUT_MS=5000
TOKEN_RATE_LIMIT_PER_MINUTE=10
IP_RATE_LIMIT_PER_MINUTE=120

# Existing PostgreSQL health probe
POSTGRES_PASSWORD=
